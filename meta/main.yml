galaxy_info:
  role_name: intel_sgx_as
  author: melhindi
  description: Install as,ld and objdump tools with mfence support to build apps with mitigation
  license: Apache-2.0
  min_ansible_version: "2.1"
  platforms:
    - name: Ubuntu
      versions:
      - focal

  galaxy_tags:
    - intel
    - sgx
    - mitigation
    - as

dependencies: []
